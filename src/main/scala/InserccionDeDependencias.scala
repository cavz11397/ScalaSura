object InserccionDeDependencias {
  def main(args: Array[String]): Unit = {

  }

  /*class Foo() { // Ugh. How could I test this? What if I ever want to use a different
    // database in another application?
    this.database = new Nothing("/path/to/my/data")
    private var database = null // We need a Database to do some work

  }

  class Foo(var database: Nothing // We need a Database to do some work
           ) // The database comes from somewhere else. Where? That's not my job, that's
  // the job of whoever constructs me: they can choose which database to use. {
*/

}
